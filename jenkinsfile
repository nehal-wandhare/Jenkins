pipeline {
    agent any

    stages {

        stage('stage-1-pull-code') {
            steps {
                git branch: 'main', url: 'https://github.com/Sagar-salve-49/EasyCRUD--1.git'
            }
        }

        stage('stage-2-build-code') {
            steps {
                echo 'build success'
            }
        }

        stage('stage-3-test-code') {
            steps {
                echo 'test success'
            }
        }

        stage('stage-4-deploy') {
            steps {
                echo 'deploy success'
            }
        }
    }
}
